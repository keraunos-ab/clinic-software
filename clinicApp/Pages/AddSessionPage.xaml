<Page x:Class="clinicApp.AddSessionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="AddSessionPage"
                       FontFamily="Constantia"
      Background="Transparent">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Text="Add New Session"
                   Grid.Row="0" Grid.ColumnSpan="2"
                   Foreground="White"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Patient Selection ComboBox -->
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,10">
            <TextBlock Text="Patient First Name"
                       Foreground="White"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <ComboBox x:Name="PatientComboBox"
                      IsEditable="True"
                      StaysOpenOnEdit="True"
                      Height="30"
                      BorderBrush="#0077FF"
                      BorderThickness="2"
                      ToolTip="Type to search patients"
                      Loaded="PatientComboBox_Loaded"
                      SelectionChanged="PatientComboBox_SelectionChanged"/>
        </StackPanel>

        <!-- Patient Last Name -->
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,10">
            <TextBlock Text="Patient Last Name"
                       Foreground="White"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <TextBox x:Name="PatientLastName"
                     Height="30"
                     BorderBrush="#0077FF"
                     BorderThickness="2"
                     ToolTip="Patient last name will auto-fill"
                     IsReadOnly="True"/>
        </StackPanel>

        <!-- Session Date -->
        <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,10,10">
            <TextBlock Text="Date"
                       Foreground="White"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <DatePicker x:Name="SessionDate"
                        BorderBrush="#0077FF"
                        BorderThickness="2"
                        ToolTip="Select session date"/>
        </StackPanel>

        <!-- Session Time -->
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="10,0,0,10">
            <TextBlock Text="Time"
                       Foreground="White"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <TextBox x:Name="SessionTime"
                     Height="30"
                     BorderBrush="#0077FF"
                     BorderThickness="2"
                     ToolTip="Enter time (e.g. 14:30)"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Margin="0,0,0,10">
            <TextBlock Text="Description"
                       Foreground="White"
                       FontWeight="SemiBold"
                       Margin="0,0,0,4"/>
            <TextBox x:Name="Description"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="100"
                     BorderBrush="#0077FF"
                     BorderThickness="2"
                     ToolTip="Optional note about the session"/>
        </StackPanel>

        <!-- Save Button -->
        <Button Grid.Row="5" Grid.ColumnSpan="2"
                Content="💾 Save"
                Background="#0077FF"
                Foreground="White"
                FontWeight="Bold"
                Height="40"
                Click="SaveSession_Click"/>
    </Grid>
</Page>
