<Window x:Class="clinicApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:clinicApp"
        mc:Ignorable="d"
        Title="Dr Belhadj F's clinic software"
        FontFamily="Constantia"
        Icon="pack://application:,,,/Pictures/icon.png"
        Height="450" Width="800"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#555555"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#777777"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseTitleBarButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F1707A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" BorderThickness="1" BorderBrush="#333333" Background="#1E1E1E">
        <DockPanel>

            <Grid DockPanel.Dock="Top" 
                  Height="45" 
                  Background="#333333" 
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                    <Image Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=Window}}" Margin="5,0" Width="18"/>
                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                               Foreground="White" 
                               VerticalAlignment="Center" 
                               Margin="5,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button Content="—" Style="{StaticResource TitleBarButton}" Click="MinimizeButton_Click"/>
                    <Button Content="☐" Style="{StaticResource TitleBarButton}" Click="MaximizeRestoreButton_Click"/>
                    <Button Content="✕" Style="{StaticResource CloseTitleBarButton}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Border Background="#2C2C2C" Grid.Column="0" Grid.ColumnSpan="2">
                    <StackPanel>
                        <TextBlock Foreground="White"
                                FontFamily="Constantia"
                                FontSize="20"
                                FontWeight="Bold"
                                Margin="20,30,0,40">
                            <Run Text="Dr Belhadj F's"/><LineBreak/><Run Text="Clinic"/>
                        </TextBlock>

                        <Button Content="🏠 L'accueil" FontFamily="Constantia" Click="HomeButton_Click"
                                Margin="10,5" Height="40" BorderBrush="#0077ff" BorderThickness="2"/>
                        <Button Content="👥 Patients" FontFamily="Constantia" Click="PatientsButton_Click"
                                Margin="10,5" Height="40" BorderBrush="#0077ff" BorderThickness="2"/>
                        <Button Content="📅 Rendez vous " FontFamily="Constantia" Click="AppointmentsButton_Click"
                                Margin="10,5" Height="40" BorderBrush="#0077ff" BorderThickness="2"/>
                        <Button Content="💊 Ordonnances " FontFamily="Constantia" Click="PrescriptionButton_Click"
                                Margin="10,5" Height="40" BorderBrush="#0077ff" BorderThickness="2"/>
                    </StackPanel>
                </Border>

                <Grid Grid.Column="2" Background="Black">
                    <Frame x:Name="MainFrame"
                           NavigationUIVisibility="Hidden"
                           Background="#1E1E1E" />
                    <Button Width="60" Height="60"
                            FontSize="28"
                            FontWeight="Bold"
                            Content="+"
                            Background="#0077ff"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="0,0,30,30"
                            Click="QuickAction_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Grid>
                                    <Ellipse Fill="{TemplateBinding Background}" />
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
                        </Button.Effect>
                    </Button>
                </Grid>
            </Grid>
        </DockPanel>
    </Border>
</Window>